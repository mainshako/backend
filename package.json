{
  "name": "button-backend",
  "version": "13.0.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "audit:report": "npm audit --json || true",
    "postinstall": "npm test && npm run audit:report"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
